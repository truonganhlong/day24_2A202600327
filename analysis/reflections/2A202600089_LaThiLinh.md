# Individual Reflection — Lab 18

**Tên:** La Thị Linh (2A202600089)
**Module phụ trách:** Module 5 (Enrichment Pipeline)

---

## 1. Đóng góp kỹ thuật

- **Module đã implement:** Module 5 - Enrichment Pipeline (Phần Technique 4: Auto Metadata Extraction & Full Enrichment Pipeline).
- **Các hàm/class chính đã viết:**
  - `extract_metadata(text)`: Hàm sử dụng LLM (`gpt-4o-mini`) để tự động trích xuất các thông tin quan trọng từ chunk text như `topic`, `entities`, `category`, `language` dưới định dạng JSON.
  - `enrich_chunks(chunks, methods)`: Hàm kết hợp tất cả các kỹ thuật làm giàu dữ liệu (summary, hypothesis questions, contextual prepend, metadata extraction) thành một pipeline hoàn chỉnh, cho phép bật/tắt linh hoạt các phương pháp qua tham số `methods`.
- **Số tests pass:** Đã test tích hợp thành công vào pipeline chung.

## 2. Kiến thức học được

- **Khái niệm mới nhất:** Pre-embedding enrichment (làm giàu dữ liệu trước khi nhúng vector). Hiểu được việc dùng LLM (như `gpt-4o-mini`) ở bước indexing là dạng "one-time cost" mang lại giá trị cao (ROI cao) vì giúp mọi truy vấn sau đó được hưởng lợi. Kỹ thuật ép LLM trả về JSON (`response_format={ "type": "json_object" }`).
- **Điều bất ngờ nhất:** Việc thêm metadata (như category, entities) hay hypothesis questions vào raw chunk giúp cải thiện đáng kể khả năng semantic match, giải quyết vấn đề raw chunks bị thiếu context so với văn bản gốc.
- **Kết nối với bài giảng (slide nào):** Slide về "Enrichment Pipeline - Tại sao cần làm giàu chunks?" và "Enrichment Techniques - 4 kỹ thuật chính".

## 3. Khó khăn & Cách giải quyết

- **Khó khăn lớn nhất:** Đảm bảo LLM luôn trả về JSON hợp lệ trong hàm `extract_metadata` để không gây lỗi khi parse (`json.loads`). Thứ hai là việc kết hợp các phương pháp enrichment sao cho logic rõ ràng, dễ bảo trì, và không gọi API dư thừa.
- **Cách giải quyết:** Áp dụng tham số `response_format={ "type": "json_object" }` của OpenAI API kết hợp với prompt chỉ dẫn rõ ràng. Bọc `try...except` để nếu LLM lỗi thì vẫn trả về dictionary rỗng `{}`, không làm sập toàn bộ indexing pipeline. Trong `enrich_chunks`, sử dụng các biến cờ (flags) như `run_metadata`, `run_summary` để kiểm tra điều kiện trước khi thực thi.
- **Thời gian debug:** Khoảng 2-3 giờ.

## 4. Nếu làm lại

- **Sẽ làm khác điều gì:** Sẽ thử nghiệm sử dụng các thư viện như `Pydantic` hoặc `Instructor` kết hợp với Structured Outputs của OpenAI để định nghĩa Schema cho Metadata một cách chặt chẽ thay vì viết prompt JSON thuần. Ngoài ra có thể thêm cơ chế retry (thử lại) nếu API bị timeout/rate limit.
- **Module nào muốn thử tiếp:** Module 4 (Evaluation) để sử dụng RAGAS đo lường cụ thể xem Pipeline Enrichment này đã giúp tăng điểm Retrieval Context Precision và Context Recall lên bao nhiêu phần trăm so với Baseline.

## 5. Tự đánh giá

| Tiêu chí | Tự chấm (1-5) |
|----------|---------------|
| Hiểu bài giảng | 5 |
| Code quality | 5 |
| Teamwork | 5 |
| Problem solving | 5 |
